<h1>Articles</h1>
<ul> <% @articles.each do |article| %>
  <li class="list-unstyled"><%= link_to article.title, article_path(article)%>
  </li>
  <% end %>
</ul>
<p> <%= link_to 'Add an article', new_article_path(@article)%> </p>
